# 用户时频偏好确立 (2026-05-10)

## 关键对话
**用户**: "现货网格时间周期没问题，主要是合约10s扫描一次，1分钟内做判断"
**用户**: "我觉得时间拉长不太对"  
**用户**: "费率套利之前的BOT止盈止损都不会一直在拉着时间"

## 用户想要的节奏

| 方面 | 旧(错) | 新(对) |
|:----|:------|:------|
| 扫描间隔 | 60秒 | **10秒** |
| 持有时长 | 4小时 | **1小时** |
| 判断周期 | 慢→错过波动 | **快进快出** |
| 止盈/止损 | 等很久才触发 | **到阈值立即执行** |
| 持仓风格 | "拖着时间" | **不拖时间, 快进快出** |

## 对V8+的参数影响
```python
SCAN_INTERVAL = 10   # 原60 → 10秒
MAX_HOLD_TIME = 3600  # 原14400(4h) → 3600(1h)
# 止盈/止损阈值保持不变, 但10秒检测一次触发更快
ADD_THRESHOLD = 0.003     # +0.3% → 10秒内可能触发
REVERSE_THRESHOLD = -0.005 # -0.5% → 10秒内可能触发
TP_THRESHOLD = 0.008      # +0.8%
```
